---
description: Review the latest changes
agent: plan
---

have @brutal-code-reviewer and @refactor-suggester check the latest changes
(check for unstaged changes first, if there are none then check the last
commit), and report on their findings. Run them in parallel to save time.

Have them report only on the changes, not the entire files. Full code review
will be done at another time. They can use the full files as context though.
